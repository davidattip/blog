/*
// app/javascript/packs/application.js

// Support pour Rails' csrf token dans les requêtes Ajax
import Rails from "@rails/ujs"
Rails.start()

// Active le support pour Stimulus dans votre application (si vous l'utilisez)
import "controllers"

// Importe les styles de Bootstrap (si vous utilisez Bootstrap)
import "bootstrap"

// Importe tous les fichiers CSS dans le dossier 'stylesheets'
import "C:\Users\david\Desktop\blog\app\assets\stylesheets\custom.css.scss"

// Importe des fichiers JavaScript ou des librairies spécifiques à partir de node_modules
// Exemple: import 'some-library'

// Active Turbolinks pour naviguer plus rapidement entre les pages (si vous l'utilisez)
import Turbolinks from "turbolinks"
Turbolinks.start()

// Active l'interaction avec les fenêtres modales de Bootstrap (si vous utilisez Bootstrap)
document.addEventListener("turbolinks:load", () => {
  $('[data-toggle="tooltip"]').tooltip()
  $('[data-toggle="popover"]').popover()
})

// Votre code JavaScript personnalisé peut être placé ici
*/
