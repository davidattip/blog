<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Your Blog", root_path, class: "navbar-brand" %>
    <div id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Articles", articles_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Users", users_path, class: "nav-link" %>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to 'Create new Article', new_article_path, class: 'nav-link' %>
          </li>
          <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to 'Create new Category', new_category_path, class: 'nav-link' %>
            </li>
          <% end %>
        <% end %>
        <% if logged_in? %>
          <li class="nav-item">
            <%= button_to 'Logout', logout_path, method: :delete, class: 'nav-link btn btn-link', form: {class: "d-inline"} %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Login', login_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', signup_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
